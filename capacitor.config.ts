import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.duoc.UniversityAsistencia',
  appName: 'University-Asistencia',
  webDir: 'www'
};

export default config;
